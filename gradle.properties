org.gradle.jvmargs=-Xmx4096m
# Build must run on JDK 8: Gradle 4.10 + ForgeGradle 2.1 target Java 8, and
# compiling on a newer JDK links against its java.nio APIs (e.g. FloatBuffer),
# producing bytecode that crashes at runtime on Java 8 with NoSuchMethodError.
# Mac build path (Zulu 8). Change if your JDK 8 lives elsewhere.
org.gradle.java.home=/Users/bory/Library/Java/JavaVirtualMachines/zulu8.94.0.17-ca-jdk8.0.492-macosx_aarch64/Contents/Home
